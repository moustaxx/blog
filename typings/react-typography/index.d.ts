declare module 'react-typography' {
	import Typography from 'typography';

	interface IProps {
		typography: Typography;
	}

	export declare const TypographyStyle = (props: IProps) => any;
	export declare const GoogleFont = (props: IProps) => any;
}
